<div class="container">
    <div class="row">
        <div class="col-8 text-center">
            <h2 class="profile-page-header">
                {{service.name}}
                20<i class="fas fa-coins"></i>
            </h2> 
            <p>{{service-description}}</p>
            <div class="col-md-5">
                <img src="./img/pp2.PNG" alt="pianist"> 
                <!-- maybe we can figure out image upload  -->
            </div>
            <div class="col-md-5">
                <h3> Training & Qualifications </h3>
                <p>{{training-qualifications}}</p>

                <h3> Availability & Preferences </h3>
                <p>{{availabitlity-prefences}}</p>
            </div>
            
            <i class="fas fa-exchange-alt">Trade</i>
            <!-- add link here -->
            
            
        </div>
        <div class="col-4">
            <img src="./img/pp2.PNG" alt="pianist"> 
                <!-- maybe we can figure out image upload/profile img -->
                <h3>{{user-name}}</h3>
                <p>{{user-bio}}</p>

                <h4>Follow Me</h4>
                <i class="fab fa-facebook"></i>
                <i class="fab fa-instagram"></i>

        </div>
            
        </div>
    </div>
</div>

  
